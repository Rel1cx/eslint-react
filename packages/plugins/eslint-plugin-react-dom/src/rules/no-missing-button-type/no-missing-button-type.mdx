---
title: no-missing-button-type
description: Enforces an explicit 'type' attribute for 'button' elements.
---

**Full Name in [`@eslint-react/eslint-plugin`](https://npmx.dev/package/@eslint-react/eslint-plugin/v/latest)**

```plain copy
@eslint-react/dom/no-missing-button-type
```

**Full Name in `eslint-plugin-react-dom`**

```plain copy
react-dom/no-missing-button-type
```

**Features**

`🔧`

**Presets**

`strict`
`strict-typescript`
`strict-type-checked`

## Rule Details

The default `type` of a button is `submit`, which causes the submission of a form when placed inside a `form` element. This is likely not the behavior you want inside a React application.

Allowed button types are `submit`, `button`, or `reset`.

## Common Violations

### Invalid

```tsx
import React from "react";

function MyComponent() {
  return <button>Click me</button>;
  //     ^^^ Missing 'type' attribute on button component.
}
```

### Valid

```tsx
import React from "react";

function MyComponent() {
  return <button type="button">Click me</button>;
}
```

## Resources

- [Rule Source](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom/src/rules/no-missing-button-type/no-missing-button-type.ts)
- [Test Source](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom/src/rules/no-missing-button-type/no-missing-button-type.spec.ts)

## Further Reading

- [MDN: `button`](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#notes)
