---
title: ref-name
---

**Full Name in `@eslint-react/eslint-plugin`**

```plain copy
@eslint-react/naming-convention/ref-name
```

**Full Name in `eslint-plugin-react-naming-convention`**

```plain copy
react-naming-convention/ref-name
```

**Presets**

`recommended`
`recommended-typescript`
`recommended-type-checked`
`strict`
`strict-typescript`
`strict-type-checked`

## Description

Enforces variable names assigned from `useRef` calls to be either `ref` or end with `Ref`.

## Examples

### Failing

```tsx
const count = useRef(0);
```

```tsx
const input = useRef<HTMLInputElement>(null);
```

```tsx
const value = useRef(null);
```

### Passing

```tsx
const ref = useRef(0);
```

```tsx
const countRef = useRef(0);
```

```tsx
const inputRef = useRef<HTMLInputElement>(null);
```

```tsx
const valueRef = useRef(null);
```

## Implementation

- [Rule Source](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-naming-convention/src/rules/ref-name.ts)
- [Test Source](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-naming-convention/src/rules/ref-name.spec.ts)
