---
title: no-direct-mutation-state
---

**Full Name in [`@eslint-react/eslint-plugin`](https://npmx.dev/package/@eslint-react/eslint-plugin/v/latest)**

```plain copy
@eslint-react/no-direct-mutation-state
```

**Full Name in [`eslint-plugin-react-x`](https://npmx.dev/package/eslint-plugin-react-x/v/latest)**

```plain copy
react-x/no-direct-mutation-state
```

**Presets**

`x`
`recommended`
`recommended-typescript`
`recommended-type-checked`
`strict`
`strict-typescript`
`strict-type-checked`

## Rule Description

Disallows direct mutation of `this.state`.

Never mutate `this.state` directly, as calling `setState()` afterward may replace the mutation you made. Treat `this.state` as if it were immutable.

The only place it's acceptable to assign `this.state` is in a class component's `constructor`.

## Triggering Examples

```tsx
import React from "react";

interface MyComponentProps {}

interface MyComponentState {
  foo: string;
}

class MyComponent extends React.Component<MyComponentProps, MyComponentState> {
  state = {
    foo: "bar",
  };

  render() {
    return (
      <div
        onClick={() => {
          this.state.foo = "baz";
          //   ^^^ Do not mutate state directly. Use 'setState()' instead.
        }}
      >
        {this.state.foo}
      </div>
    );
  }
}
```

## Non Triggering Examples

```tsx
import React from "react";

interface MyComponentProps {}

interface MyComponentState {
  foo: string;
}

class MyComponent extends React.Component<MyComponentProps, MyComponentState> {
  state = {
    foo: "bar",
  };

  render() {
    return (
      <div
        onClick={() => {
          this.setState({ foo: "baz" });
        }}
      >
        {this.state.foo}
      </div>
    );
  }
}
```

## Resources

- [Rule Source](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x/src/rules/no-direct-mutation-state/no-direct-mutation-state.ts)
- [Test Source](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x/src/rules/no-direct-mutation-state/no-direct-mutation-state.spec.ts)

---

## See Also

- [`no-access-state-in-setstate`](./no-access-state-in-setstate)\
  Disallows accessing  inside  calls
- [`no-unused-state`](./no-unused-state)\
  Warns about unused class component state
- [`no-set-state-in-component-did-mount`](./no-set-state-in-component-did-mount)\
  Disallows calling  in  outside functions such as callbacks