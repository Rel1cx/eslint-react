[@eslint-react/core](../README.md) / isUseLayoutEffectCall

# Variable: isUseLayoutEffectCall

```ts
const isUseLayoutEffectCall: ReturnType;
```

Check if the node is a call expression to the React `useLayoutEffect` API.
