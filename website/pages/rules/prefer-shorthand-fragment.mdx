# prefer-shorthand-fragment

## Rule category

Style.

## What it does

Enforces the usage of `<></>` over `<React.Fragment></React.Fragment>`.

## Why is this good?

`<></>` is shorter and more readable. And it does not require importing `React` or the `Fragment` component.

## Examples

### Failing

```tsx twoslash
import React from "react";

function Example() {
  return (
// @warn: Use shorthand fragment syntax instead
    <React.Fragment>
      <button />
      <button />
    </React.Fragment>
  );
}
```

### Passing

```tsx twoslash
import React from "react";

function Example() {
  return (
    <>
      <button />
      <button />
    </>
  );
}
```
