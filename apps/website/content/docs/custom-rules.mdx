---
title: Custom Rules
description: Build custom ESLint rules for React using the eslint-react toolkit.
---

<Callout type="warning">
  This page is under construction. The API and examples provided here are subject to change as we continue to develop the plugin. Please check back later for updates.
</Callout>

<Callout type="info">
  This plugin is for advanced use cases where you need to build custom ESLint rules tailored to your project's specific requirements.
</Callout>

## Installation

```bash copy
npm install --save-dev eslint-plugin-react-kit
```

## Usage

The `eslint-plugin-react-kit` package provides a `definePlugin` function that allows you to create custom ESLint rules powered by the eslint-react toolkit.

```ts copy
import { definePlugin, defineRuleListener } from "eslint-plugin-react-kit";

const plugin = definePlugin([
  {
    name: "function-component-definition",
    make: (context, toolkit) => {
      const { api, visitor } = toolkit.getComponentCollector(context);

      return defineRuleListener(visitor, {
        "Program:exit"(program) {
          for (const { node } of api.getAllComponents(program)) {
            if (node.type === "ArrowFunctionExpression") continue;
            context.report({
              node,
              message: "Function components must be defined with arrow functions.",
            });
          }
        },
      });
    },
  },
]);

export default plugin;
```

## Resources

- [AST Explorer](https://astexplorer.net/) - A tool for exploring the abstract syntax tree (AST) of JavaScript code, which is essential for writing ESLint rules.
