---
title: context-name
description: Enforces the context name to be a valid component name with the suffix 'Context'.
---

**Full Name in `eslint-plugin-react-naming-convention`**

```plain copy
react-naming-convention/context-name
```

**Full Name in [`@eslint-react/eslint-plugin`](https://npmx.dev/package/@eslint-react/eslint-plugin/v/latest)**

```plain copy
@eslint-react/naming-convention-context-name
```

**Presets**

`recommended`
`recommended-typescript`
`recommended-type-checked`
`strict`
`strict-typescript`
`strict-type-checked`

## Rule Details

Enforces that the context has a valid component name with the suffix `Context`.

## Examples

### Naming context without the `Context` suffix

```tsx
// Problem: Context name should end with 'Context'
const theme = createContext("");
```

```tsx
// Problem: Context name should end with 'Context'
const Theme = createContext("");
```

```tsx
// Problem: Context name should be PascalCase and end with 'Context'
const themecontext = createContext("");
```

```tsx
// Problem: Context name should be PascalCase and end with 'Context'
const themeContext = createContext("");
```

```tsx
// Problem: Context name should be PascalCase and end with 'Context'
const context = createContext("");
```

### Naming context correctly

```tsx
// Recommended: PascalCase with 'Context' suffix
const ThemeContext = createContext("");
```

```tsx
// Recommended: PascalCase with 'Context' suffix
const Context = createContext("");
```

## Resources

- [Rule Source](https://github.com/Rel1cx/eslint-react/tree/main/plugins/eslint-plugin-react-naming-convention/src/rules/context-name/context-name.ts)
- [Test Source](https://github.com/Rel1cx/eslint-react/tree/main/plugins/eslint-plugin-react-naming-convention/src/rules/context-name/context-name.spec.ts)
- [Rule Changelog](https://github.com/Rel1cx/eslint-react/tree/main/plugins/eslint-plugin-react-naming-convention/src/rules/context-name/CHANGELOG.md)

## Further Reading

- [React Docs: Context](https://react.dev/learn/passing-data-deeply-with-context)
- [React Docs: `createContext`](https://react.dev/reference/react/createContext)
